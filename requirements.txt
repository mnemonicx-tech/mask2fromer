# ──────────────────────────────────────────────────────────────────
# requirements.txt — Mask2Former Fashion Training
# Python 3.10 | CUDA 12.2 | Ubuntu
# ──────────────────────────────────────────────────────────────────

# PyTorch (install separately via conda or pip wheel for CUDA 12.2)
# torch==2.2.2+cu121
# torchvision==0.17.2+cu121

# Detectron2 — install from source (see setup.sh)
# git+https://github.com/facebookresearch/detectron2.git

# Mask2Former — install from source (see setup.sh)
# git+https://github.com/facebookresearch/Mask2Former.git

# Core vision / data
opencv-python-headless>=4.9.0
Pillow>=10.2.0
pycocotools>=2.0.7
numpy>=1.26.0

# Training utilities
fvcore>=0.1.5.post20221221
iopath>=0.1.10
omegaconf>=2.3.0
hydra-core>=1.3.2
timm>=0.9.16                 # for Swin backbone

# Logging
tensorboard>=2.16.0
wandb>=0.16.6                # optional WandB logging

# Misc
scipy>=1.12.0
tqdm>=4.66.2
termcolor>=2.4.0
packaging>=24.0
cloudpickle>=3.0.0
